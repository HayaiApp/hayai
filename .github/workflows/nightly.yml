name: Nightly

on:
  push:
    branches:
      - master
  workflow_dispatch:

concurrency:
  group: hayai-nightly
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build and publish nightly
    runs-on: ubuntu-24.04
    environment: build

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          java-version: 21
          distribution: temurin

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0

      - name: Copy CI gradle.properties
        run: |
          mkdir -p ~/.gradle
          cp .github/runner-files/ci-gradle.properties ~/.gradle/gradle.properties
          mkdir -p app/src/dev
          cp app/src/standard/google-services.json app/src/dev/google-services.json

      - name: Choose nightly number
        env:
          GH_TOKEN: ${{ secrets.NIGHTLY_PAT }}
        shell: bash
        run: |
          set -euo pipefail
          latest_release=$(gh api repos/HayaiApp/hayai-nightly/releases/latest)
          latest_tag=$(jq -r '.tag_name' <<< "$latest_release")
          if [[ ! "$latest_tag" =~ ^r([0-9]+)$ ]]; then
            echo "Unexpected latest nightly tag: $latest_tag" >&2
            exit 1
          fi
          next_remote=$((BASH_REMATCH[1] + 1))
          commit_count=$(git rev-list --count HEAD)
          if (( commit_count > next_remote )); then
            build_number=$commit_count
          else
            build_number=$next_remote
          fi
          echo "NIGHTLY_BUILD_NUMBER=$build_number" >> "$GITHUB_ENV"
          echo "VERSION_TAG=r$build_number" >> "$GITHUB_ENV"
          echo "COMMIT_SHORT_SHA=$(git rev-parse --short HEAD)" >> "$GITHUB_ENV"
          latest_body=$(jq -r '.body // ""' <<< "$latest_release")
          if [[ "$latest_body" =~ HayaiApp/hayai/commit/([0-9a-fA-F]{40}) ]]; then
            echo "PREVIOUS_NIGHTLY_SHA=${BASH_REMATCH[1]}" >> "$GITHUB_ENV"
          fi

      - name: Generate nightly changelog
        shell: pwsh
        run: >-
          ./tools/new-nightly-changelog.ps1
          -PreviousSha '${{ env.PREVIOUS_NIGHTLY_SHA }}'
          -CurrentSha '${{ github.sha }}'
          -OutputPath nightly-release-notes.md

      - name: Build and test
        # This project only creates a unit-test component for DevDebug. Exercise
        # the shared production code through that existing test variant while
        # assembling the signed StandardNightly artifacts.
        run: ./gradlew assembleStandardNightly testDevDebugUnitTest -x formatKotlin

      - name: Detect Android build tools
        shell: bash
        run: |
          build_tools_version=$(find "$ANDROID_SDK_ROOT/build-tools" -mindepth 1 -maxdepth 1 -printf '%f\n' | sort -V | tail -1)
          echo "BUILD_TOOLS_VERSION=$build_tools_version" >> "$GITHUB_ENV"

      - name: Sign APKs
        uses: r0adkll/sign-android-release@v1
        with:
          releaseDirectory: app/build/outputs/apk/standard/nightly
          signingKeyBase64: ${{ secrets.SIGNING_KEY }}
          alias: ${{ secrets.ALIAS }}
          keyStorePassword: ${{ secrets.KEY_STORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: ${{ env.BUILD_TOOLS_VERSION }}

      - name: Verify signed minified APK startup on Android 12
        uses: ReactiveCircus/android-emulator-runner@c9c93e6ba9c4194322e114c40970d5983d9a07cc # v2.38.0
        with:
          api-level: 31
          arch: x86_64
          target: default
          disable-animations: true
          script: >-
            pwsh -File tools/verify-apk-startup.ps1
            -Apk app/build/outputs/apk/standard/nightly/app-standard-x86_64-nightly-unsigned-signed.apk
            -PackageId dev.ahmedmohamed.hayai.nightly

      - name: Upload startup diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nightly-startup-${{ github.sha }}-${{ github.run_attempt }}
          path: |
            artifacts/apk-startup/
            app/build/outputs/apk/standard/nightly/app-standard-x86_64-nightly-unsigned-signed.apk
          retention-days: 3

      - name: Prepare release assets
        shell: bash
        run: |
          set -euo pipefail
          dir=app/build/outputs/apk/standard/nightly
          mv "$dir/app-standard-universal-nightly-unsigned-signed.apk" "hayai-${VERSION_TAG}.apk"
          cp "$dir/app-standard-arm64-v8a-nightly-unsigned-signed.apk" "hayai-arm64-v8a-${VERSION_TAG}.apk"
          cp "$dir/app-standard-armeabi-v7a-nightly-unsigned-signed.apk" "hayai-armeabi-v7a-${VERSION_TAG}.apk"
          cp "$dir/app-standard-x86-nightly-unsigned-signed.apk" "hayai-x86-${VERSION_TAG}.apk"
          cp "$dir/app-standard-x86_64-nightly-unsigned-signed.apk" "hayai-x86_64-${VERSION_TAG}.apk"
          for apk in hayai-*-${VERSION_TAG}.apk hayai-${VERSION_TAG}.apk; do
            sha256sum "$apk"
          done | tee nightly-checksums.txt

      - name: Publish nightly
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        with:
          repository: HayaiApp/hayai-nightly
          tag_name: ${{ env.VERSION_TAG }}
          name: Hayai Nightly ${{ env.VERSION_TAG }}
          body_path: nightly-release-notes.md
          files: |
            hayai-${{ env.VERSION_TAG }}.apk
            hayai-arm64-v8a-${{ env.VERSION_TAG }}.apk
            hayai-armeabi-v7a-${{ env.VERSION_TAG }}.apk
            hayai-x86-${{ env.VERSION_TAG }}.apk
            hayai-x86_64-${{ env.VERSION_TAG }}.apk
            nightly-checksums.txt
          draft: false
          prerelease: false
          token: ${{ secrets.NIGHTLY_PAT }}

      - name: Upload mapping
        uses: actions/upload-artifact@v4
        with:
          name: nightly-mapping-${{ github.sha }}
          path: app/build/outputs/mapping/standardNightly
